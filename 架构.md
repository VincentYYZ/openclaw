# OpenClaw — 架构分析

## 项目定位

**OpenClaw** — 开源个人 AI 助手平台。TypeScript/Node.js，pnpm monorepo，MIT 许可。

核心特性：20+ 消息渠道、插件化架构（plugin-sdk）、多模型 Provider、安全优先、多 Agent 支持。

---

## 整体架构

```
┌───────────────────────────────────────────────────────┐
│  入口层: CLI (src/cli/) │ Gateway (src/gateway/) │ Library │
├───────────────────────────────────────────────────────┤
│  命令层: src/commands/ (onboard/doctor/configure/...)  │
├───────────────────────────────────────────────────────┤
│  Agent层: src/agents/ (推理/模型/工具/压缩/子Agent/沙箱) │
├───────────────────────────────────────────────────────┤
│  插件层: src/plugins/ + src/plugin-sdk/ (manifest/加载/注册) │
├───────────────────────────────────────────────────────┤
│  渠道层: src/channels/ + extensions/ (20+渠道适配)     │
├───────────────────────────────────────────────────────┤
│  基础设施: src/infra/ + src/config/ (配置/审批/安全/更新) │
└───────────────────────────────────────────────────────┘
```

---

## 核心模块

### 1. 入口层

- `src/entry.ts` — CLI 主入口：环境初始化、profile 解析、respawn、版本快速路径
- `src/index.ts` — Library 入口：导出 `loadConfig`/`runExec`/`loadSessionStore` 等公共 API
- 启动流程：`openclaw.mjs → entry.ts → normalizeEnv → runCli() → gateway/CLI mode`

### 2. CLI 层 — `src/cli/`

Commander.js 命令体系，关键子模块：

| 子模块 | 职责 |
|---|---|
| `program/` | 命令定义（73 文件） |
| `capability-cli.ts` | 能力管理（67KB） |
| `config-cli.ts` | 配置读写（48KB） |
| `plugins-cli.ts` | 插件安装/卸载/更新 |
| `daemon-cli/` | 守护进程管理 |
| `gateway-cli/` | Gateway 启停 |
| `cron-cli/` | 定时任务 |
| `models-cli.ts` | 模型选择/切换 |
| `skills-cli.ts` | 技能管理 |
| `secrets-cli.ts` | 密钥管理 |

### 3. 命令层 — `src/commands/`

高层业务逻辑，CLI 和 Gateway 共享：

| 子模块 | 职责 |
|---|---|
| `onboard*.ts` | 引导式安装向导 |
| `doctor/` | 诊断修复（70 文件：auth/config/legacy/sandbox/security/state） |
| `configure*.ts` | 配置向导 |
| `models/` | 模型选择/认证/列表 |
| `channels/` | 渠道添加/移除/状态 |
| `agents.*.ts` | 多 Agent 管理 |
| `status*.ts` | 系统状态 |
| `auth-choice*.ts` | Provider 认证选择 |

### 4. Gateway 层 — `src/gateway/`

核心服务端，处理 WS 连接/认证/会话/工具调度：

| 子模块 | 职责 |
|---|---|
| `server.impl.ts` | 主服务（30KB） |
| `server-http.ts` | HTTP 端点（42KB） |
| `server-chat.ts` | 聊天处理（33KB） |
| `server-channels.ts` | 渠道运行时（28KB） |
| `server-plugins.ts` | 插件引导（17KB） |
| `server-cron.ts` | 定时调度（20KB） |
| `net.ts` | 网络/端口/TLS（16KB） |
| `auth.ts` | 认证系统（20KB） |
| `client.ts` | 客户端连接（31KB） |
| `hooks.ts` | Hook 系统（15KB） |
| `protocol/` | 类型化 Gateway 协议（33 文件） |
| `openai-http.ts` | OpenAI 兼容 API（22KB） |
| `openresponses-http.ts` | OpenAI Responses API（35KB） |
| `mcp-http.ts` | MCP HTTP 端点 |
| `control-ui.ts` | Web 控制面板（20KB） |
| `session-utils.ts` | 会话工具（45KB） |

### 5. Agent 层 — `src/agents/`

推理与执行核心（1311 文件，最大模块）：

| 子模块 | 职责 |
|---|---|
| `pi-embedded-runner/` | 嵌入式 Agent 运行器（183 文件） |
| `pi-embedded-subscribe.ts` | 流式订阅（31KB） |
| `pi-tools.ts` | 工具定义/注册（27KB） |
| `system-prompt.ts` | 系统提示词组装（43KB） |
| `agent-command.ts` | Agent 命令（38KB） |
| `openai-transport-stream.ts` | OpenAI 流式传输（51KB） |
| `openai-ws-stream.ts` | OpenAI WS 流（46KB） |
| `anthropic-transport-stream.ts` | Anthropic 流式传输（29KB） |
| `google-transport-stream.ts` | Gemini 流式传输（24KB） |
| `model-fallback.ts` | 模型故障转移（31KB） |
| `model-selection.ts` | 模型选择（27KB） |
| `compaction.ts` | 上下文压缩（19KB） |
| `subagent-*.ts` | 子 Agent 系统（spawn/registry/lifecycle/control） |
| `bash-tools.exec.ts` | Bash 执行（56KB） |
| `sandbox/` | 沙箱系统（71 文件） |
| `skills/` | 技能系统（27 文件） |
| `tools/` | Agent 工具集（111 文件） |
| `auth-profiles/` | 认证配置（36 文件） |

**Agent 运行流程**：
```
用户消息 → channel inbound → session → agent scope
  → system prompt → model selection → provider stream
  → tool call → hooks → tool exec → hooks
  → compaction → reply → channel outbound
```

### 6. 插件系统 — `src/plugins/` + `src/plugin-sdk/`

**最核心架构特性**，严格分层：

**`src/plugin-sdk/`** — 公共 SDK（扩展可导入）：
- `core.ts` — 核心运行时 API（24KB）
- `plugin-entry.ts` / `provider-entry.ts` — 插件/Provider 入口
- `channel-contract.ts` — 渠道合约
- `channel-*.ts` — 各渠道 SDK（telegram/discord/slack/whatsapp/feishu/matrix/...）
- `provider-*.ts` — Provider SDK（auth/catalog/model/stream/tools）
- `approval-*.ts` — 审批 SDK
- `browser-*.ts` — 浏览器 SDK
- `memory-*.ts` — 记忆 SDK
- `facade-runtime.ts` — Facade 加载（12KB）

**`src/plugins/`** — 插件基础设施（核心内部）：
- `manifest.ts` — Manifest 解析/验证（31KB）
- `discovery.ts` — 插件发现（28KB）
- `loader.ts` — 插件加载器（87KB，**项目最大文件**）
- `registry.ts` — 注册中心（49KB）
- `install.ts` — 安装（33KB）
- `marketplace.ts` — ClawHub 市场（34KB）
- `hooks.ts` — Hook 系统（37KB）
- `types.ts` — 类型定义（71KB）
- `contracts/` — 合约定义（89 文件）

**架构规则**：
- 扩展只能导入 `openclaw/plugin-sdk/*`，禁止 `src/**`
- 核心不硬编码扩展信息，通过 manifest/registry/capability
- 新增插件不需改核心代码
- 插件通过 `openclaw.plugin.json` manifest 声明

### 7. 捆绑插件 — `extensions/`

100+ 官方捆绑插件：

| 类别 | 示例 |
|---|---|
| **Provider (40+)** | openai, anthropic, google, ollama, openrouter, groq, mistral, deepseek, nvidia, minimax, moonshot, qwen, amazon-bedrock, anthropic-vertex, codex, github-copilot, litellm, ... |
| **Channel (20+)** | telegram, discord, slack, whatsapp, signal, imessage, matrix, feishu, msteams, googlechat, irc, line, qqbot, zalo, wechat, ... |
| **Memory** | memory-core, memory-lancedb, memory-wiki, active-memory |
| **Search** | duckduckgo, exa, tavily, searxng, brave, firecrawl |
| **Media** | image-generation-core, video-generation-core, fal, comfy, elevenlabs, deepgram, speech-core |
| **Tool** | browser, diffs, phone-control, webhooks, voice-call, talk-voice, acpx |

### 8. 渠道层 — `src/channels/`

- `plugins/` — 渠道插件适配器（180 文件）
- `session.ts` — 会话管理
- `allowlist-match.ts` — 消息允许列表
- `command-gating.ts` — 命令门控
- `thread-bindings-*.ts` — 线程绑定
- `typing.ts` — 打字状态
- `status-reactions.ts` — 状态反应（12KB）
- `sender-identity.ts` — 发送者身份

### 9. 基础设施 — `src/infra/`

| 子模块 | 职责 |
|---|---|
| `exec-approvals*.ts` | 命令审批系统（多文件，核心安全特性） |
| `heartbeat-*.ts` | 心跳/定时提醒（52KB runner） |
| `device-pairing.ts` | 设备配对（32KB） |
| `update-runner.ts` | 自更新（40KB） |
| `state-migrations.ts` | 状态迁移（39KB） |
| `restart-*.ts` | 重启管理 |
| `session-cost-usage.ts` | 会话费用统计（34KB） |
| `fs-safe.ts` | 安全文件操作（35KB） |
| `outbound/` | 出站消息（109 文件） |
| `net/` | 网络工具（18 文件） |
| `tls/` | TLS 配置 |
| `bonjour.ts` | Bonjour/mDNS 发现（14KB） |
| `tailscale.ts` | Tailscale 集成（16KB） |
| `push-apns.ts` | iOS 推送（33KB） |

### 10. 配置系统 — `src/config/`

| 子模块 | 职责 |
|---|---|
| `schema.ts` + `schema.base.generated.ts` | Zod Schema（1.3MB 生成文件） |
| `schema.help.ts` | 配置帮助文本（162KB） |
| `schema.labels.ts` | 配置标签（51KB） |
| `io.ts` | 配置读写（63KB） |
| `validation.ts` | 配置验证（28KB） |
| `types.*.ts` | 类型定义（20+ 文件） |
| `zod-schema.*.ts` | Zod Schema 定义（多文件） |
| `redact-snapshot.ts` | 配置脱敏（28KB） |
| `plugin-auto-enable.ts` | 插件自动启用 |
| `sessions/` | 会话配置（47 文件） |

### 11. 其他核心模块

| 模块 | 职责 |
|---|---|
| `src/context-engine/` | 上下文引擎（注册/委托/类型） |
| `src/sessions/` | 会话 ID/生命周期/转录 |
| `src/cron/` | 定时任务（150 文件） |
| `src/terminal/` | 终端管理 |
| `src/tui/` | TUI 界面 |
| `src/tts/` | TTS 语音合成 |
| `src/media/` | 媒体处理管道 |
| `src/image-generation/` | 图像生成 |
| `src/video-generation/` | 视频生成 |
| `src/music-generation/` | 音乐生成 |
| `src/media-understanding/` | 媒体理解（视觉） |
| `src/web-search/` | Web 搜索 |
| `src/web-fetch/` | Web 抓取 |
| `src/mcp/` | MCP 集成 |
| `src/acp/` | ACP 服务器（IDE 集成） |
| `src/canvas-host/` | 实时 Canvas |
| `src/realtime-voice/` | 实时语音 |
| `src/realtime-transcription/` | 实时转写 |
| `src/secrets/` | 密钥管理 |
| `src/security/` | 安全策略 |
| `src/hooks/` | Hook 系统 |
| `src/routing/` | 消息路由 |
| `src/pairing/` | 设备配对 |
| `src/process/` | 进程管理 |
| `src/daemon/` | 守护进程 |
| `src/status/` | 状态报告 |
| `src/tasks/` | 任务管理 |
| `src/shared/` | 共享工具 |
| `src/utils/` | 通用工具 |

---

## 依赖调用链

```
src/plugin-sdk/core.ts  ← 扩展唯一可导入的公共面
       ↑
extensions/*/           ← 各插件通过 plugin-sdk 访问核心
       ↑
src/plugins/loader.ts   ← 加载/验证/注册所有插件
       ↑
src/agents/             ← Agent 运行时通过 registry 使用插件能力
       ↑
src/gateway/            ← Gateway 调度 Agent + 管理连接
       ↑
src/cli/ + src/commands/ ← CLI/Gateway 入口
```

---

## 配置体系

- **配置文件**：YAML（`openclaw.yaml`），Zod Schema 验证
- **Schema 生成**：`schema.base.generated.ts`（1.3MB）
- **密钥管理**：`src/secrets/` + `SecretRef` 语义
- **迁移**：`src/infra/state-migrations.ts` + `openclaw doctor --fix`
- **运行时快照**：`config/runtime-snapshot.ts` + 脱敏

---

## 安全体系

| 特性 | 实现 |
|---|---|
| 命令审批 | `exec-approvals` — 危险命令需人工确认 |
| 密钥脱敏 | `redact-snapshot` — 日志/状态中隐藏 API key |
| SSRF 防护 | `plugin-sdk/ssrf-policy.ts` — 限制内网请求 |
| 主机环境安全 | `infra/host-env-security.ts` — 检测危险环境变量 |
| 路径安全 | `infra/path-guards.ts` + `plugins/path-safety.ts` |
| 认证 | Gateway token + 设备配对 + OAuth |
| 速率限制 | `auth-rate-limit.ts` + `control-plane-rate-limit.ts` |
| 插件安全扫描 | `install-security-scan.ts` — 安装前检查 |

---

## 与 Hermes Agent 的关键架构差异

| 维度 | OpenClaw | Hermes Agent |
|---|---|---|
| 语言 | TypeScript (async) | Python (sync) |
| 架构 | 插件化，严格边界 | 单体，无边界 |
| 插件 | manifest 驱动，plugin-sdk 公共面 | Python register()，无隔离 |
| Agent 循环 | 异步流式（OpenAI/Anthropic/Google transport） | 同步循环 |
| 配置 | Zod Schema + YAML，1.3MB 生成文件 | config.yaml + .env |
| 安全 | 多层审批/脱敏/SSRF/路径防护 | approval.py 危险命令检测 |
| 渠道 | 20+，插件式 | 17+，内置式 |
| Provider | 40+ 插件 | 200+ 模型 OpenRouter |
| 子 Agent | 深度限制/生命周期/注册中心 | delegate_task 隔离 |
| 扩展方式 | 写插件，不改核心 | 改核心代码 |





